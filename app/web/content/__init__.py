"""The words of the site, one module per language: en.py, hi.py, mr.py (see app/web/pages.py for the structure).

Each module is written to its OWN language's keywords - do not "sync" them by translating one into another.
"""
